---
cisco:
  platform: ios

arista:
  platform: eos
  connection_options:
    netmiko:
      extras:
        global_delay_factor: 5
        # session_log: "arista.txt"

juniper:
  platform: junos
  data:
    arp_cmd: "show arp"

nxos:
  platform: nxos
  data:
    arp_cmd: "show ip arp vrf management"
